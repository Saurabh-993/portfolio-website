---
/**
 * Page4_Process.astro — placeholder scroll section (100dvh).
 * TODO: how I work / process breakdown.
 */
---

<section class="section placeholder" id="process">
  <p class="label serif">Process — coming soon</p>
</section>

<style>
  .placeholder {
    display: grid;
    place-items: center;
  }
  .label {
    font-size: clamp(1.2rem, 2.5vw, 2rem);
    color: var(--text-faint);
    font-style: italic;
  }
</style>
