---
// Page2_Work.astro — clean professional About/Skills bento section.
import ParticleMorph from "../components/ui/ParticleMorph.astro";
import TechDeck from "../components/ui/TechDeck.astro";

const tech = [
  { label: "JavaScript", slug: "javascript" },
  { label: "TypeScript", slug: "typescript" },
  { label: "Python",     slug: "python" },
  { label: "Node.js",    slug: "nodedotjs" },
  { label: "Express",    slug: "express" },
  { label: "FastAPI",    slug: "fastapi" },
  { label: "REST APIs" },
  { label: "MongoDB",    slug: "mongodb" },
  { label: "PostgreSQL", slug: "postgresql" },
  { label: "MySQL",      slug: "mysql" },
  { label: "React",      slug: "react" },
  { label: "LangChain",  slug: "langchain" },
  { label: "LangGraph" },
  { label: "n8n",        slug: "n8n" },
  { label: "Git",        slug: "git" },
  { label: "GitHub",     slug: "github" },
];
const loop = [...tech, ...tech];
---

<section class="p2" id="work">
  <!-- page1 → page2 boundary: full-width smooth gradient band -->
  <div class="p2__band" aria-hidden="true"></div>

  <!-- Ambient background glow that triggers on hover -->
  <div class="p2__glow" aria-hidden="true"></div>

  <div class="p2__inner">
   <!-- horizontal track: window 1 (bento) · window 2 (tech deck) -->
   <div class="p2-track" data-p2-track>
   <div class="p2-panel">
   <div class="mac-window">
    <div class="mac-header">
      <div class="mac-buttons">
        <span class="mac-btn close"></span>
        <span class="mac-btn minimize"></span>
        <span class="mac-btn maximize"></span>
      </div>
      <div class="mac-action">
        <svg width="12" height="12" viewBox="0 0 12 12" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M6 1V11M1 6H11" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
      </div>
    </div>
    <div class="mac-body">
      <div class="bento">

        <article class="card about">
          <div class="label"><span class="dot"></span>ABOUT</div>
          <div class="about__head">
            <div class="avatar">S</div>
            <h2>Hi, I'm Saurabh.</h2>
          </div>
          <p class="bio">I build at the intersection of engineering and design — interfaces that feel
            alive, systems that stay simple underneath, and the kind of polish people only notice when
            it's missing.</p>
          <dl class="facts">
            <div><dt>Currently</dt><dd>creative frontend &amp; real-time graphics</dd></div>
            <div><dt>Focus</dt><dd>engineering &times; design, built end to end</dd></div>
            <div><dt>Edge</dt><dd>AI-augmented — I can learn, build &amp; research almost anything</dd></div>
          </dl>
        </article>

        <article class="card tech">
          <div class="label"><span class="dot"></span>STACK</div>
          <h2>Tech I build with</h2>
          <p class="bio">The stack I've gone deep on — what I reach for when an idea has to become real
            software, from interface to API to database. And with AI in the loop, I can pick up whatever
            a problem demands and ship it.</p>
          <div class="marquee">
            <div class="marquee__track">
              {loop.map((t) => (
                <span class="chip">
                  {t.slug && <img src={`https://cdn.simpleicons.org/${t.slug}`} alt="" width="18" height="18" loading="lazy" />}
                  {t.label}
                </span>
              ))}
            </div>
          </div>

          <!-- → slides over to the tech-deck window -->
          <a class="deck-link" href="#tech-deck" data-to-deck>
            Tech Stack<span class="dl-chev">›</span><span class="dl-chev dl-c2">›</span><span class="dl-chev dl-c3">›</span>
          </a>
        </article>

        <article class="card play">
          <div class="label"><span class="dot dot--blue"></span>PLAYGROUND</div>
          <div class="play__panel">
            <ParticleMorph />
          </div>
        </article>

      </div>
    </div>
   </div>
   </div>

   <!-- ============ WINDOW 2: the tech-stack deck (sideways) ============ -->
   <div class="p2-panel" id="tech-deck">
   <div class="mac-window">
    <div class="mac-header">
      <div class="mac-buttons">
        <span class="mac-btn close"></span>
        <span class="mac-btn minimize"></span>
        <span class="mac-btn maximize"></span>
      </div>
      <div class="deck-title">what I actually work with</div>
      <div class="mac-action">
        <svg width="12" height="12" viewBox="0 0 12 12" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M6 1V11M1 6H11" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
      </div>
    </div>
    <div class="mac-body">
      <TechDeck />
    </div>
   </div>
   </div>

   </div><!-- /p2-track -->
  </div>

  <!-- page2 → page3 boundary: aurora lower half (mirrors p3__seam above) -->
  <div class="p2__seam" aria-hidden="true"></div>
</section>

<style>
  :root, html[data-theme='light'] {
    --p2-bg: #fbfbfa; /* clean professional near-white (ref 3's lower field) */
    --mac-bg: #ffffff;
    --mac-header-bg: linear-gradient(to bottom, #f5f5f7, #e8e8ed);
    --mac-border: rgba(0, 0, 0, 0.12);
    --mac-shadow: 0 20px 60px rgba(0,0,0,0.08), 0 0 0 1px rgba(0,0,0,0.03);
    --mac-icon: #86868b;
  }

  :global(html[data-theme='dark']) {
    --p2-bg: #0a0a0a; /* Exact deep dark background matching Image 2 */
    --mac-bg: #111111;
    --mac-header-bg: linear-gradient(to bottom, #323232, #1e1e1e);
    --mac-border: rgba(255, 255, 255, 0.12);
    --mac-shadow: 0 30px 80px rgba(0,0,0,0.6), inset 0 1px 1px rgba(255,255,255,0.15);
    --mac-icon: #7d7d7d;
  }

  .p2 {
    background: var(--p2-bg);
    color: var(--text);
    position: relative;
    height: 100dvh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    transition: background-color 0.3s ease;
  }

  /* ================= DARK MODE: de-purpled, neutral + cool =================
     Scoped re-definition of the site vars INSIDE .p2 only — the bento goes
     neutral dark with cyan/blue accents (refs 3 & 4), never violet. */
  :global(html[data-theme='dark']) .p2 {
    --card-bg: #131313;
    --glass-border: rgba(255, 255, 255, 0.09);
    --grid-line: rgba(255, 255, 255, 0.07);
    --accent: #22d3ee;                      /* cyan */
    --accent-glow: rgba(34, 211, 238, 0.25);
    --accent-soft: rgba(34, 211, 238, 0.1);
    --ball-a: #67e8f9;                      /* avatar gradient: cyan → blue */
    --ball-b: #2563eb;
    --text: #f4f6f8;
    --text-muted: rgba(235, 240, 248, 0.62);
    --text-faint: rgba(235, 240, 248, 0.32);
  }

  /* ================= boundary gradient band (top of page 2) ================= */
  .p2__band {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: clamp(120px, 20vh, 220px);
    pointer-events: none;
    z-index: 0;
    /* DAY — organic gold → white. Like hiartem's band, the hue varies
       HORIZONTALLY too: gold pools left + right, a softer honey glow in
       the center — overlapping radials under a vertical dissolve, so it
       reads like light, not like a CSS gradient. */
    background:
      radial-gradient(58% 150% at 8% -30%,  rgba(242, 195, 74, 0.95) 0%, rgba(242, 195, 74, 0) 62%),
      radial-gradient(70% 170% at 48% -45%, rgba(250, 228, 158, 0.85) 0%, rgba(250, 228, 158, 0) 66%),
      radial-gradient(55% 140% at 90% -25%, rgba(238, 186, 62, 0.9) 0%, rgba(238, 186, 62, 0) 60%),
      linear-gradient(to bottom, #faf6ef 0%, rgba(250, 246, 239, 0) 65%);
    -webkit-mask-image: linear-gradient(to bottom, #000 0%, #000 30%, transparent 100%);
    mask-image: linear-gradient(to bottom, #000 0%, #000 30%, transparent 100%);
  }
  /* NIGHT — ref 3's aurora, horizontally alive: soft green breathing on
     the left, teal center, deep navy pooling right — dissolving into the
     page's near-black. */
  :global(html[data-theme='dark']) .p2__band {
    background:
      radial-gradient(55% 160% at 6% -30%,  rgba(96, 214, 148, 0.5) 0%, rgba(96, 214, 148, 0) 60%),
      radial-gradient(65% 170% at 42% -40%, rgba(28, 168, 152, 0.55) 0%, rgba(28, 168, 152, 0) 64%),
      radial-gradient(70% 170% at 92% -35%, rgba(35, 66, 150, 0.7) 0%, rgba(35, 66, 150, 0) 65%),
      linear-gradient(to bottom, #1a1030 0%, rgba(16, 12, 28, 0) 70%);
    -webkit-mask-image: linear-gradient(to bottom, #000 0%, #000 28%, transparent 100%);
    mask-image: linear-gradient(to bottom, #000 0%, #000 28%, transparent 100%);
  }

  /* ================= boundary gradient band (bottom of page 2 → page 3) ================= */
  .p2__seam {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: clamp(140px, 24vh, 260px);
    pointer-events: none;
    z-index: 0;
    /* sage → cyan → blue pools anchored toward the junction (bottom edge) */
    background:
      radial-gradient(58% 150% at 8% 130%,  rgba(118, 198, 160, 0.72) 0%, rgba(118, 198, 160, 0) 62%),
      radial-gradient(70% 170% at 48% 145%, rgba(94, 210, 235, 0.78) 0%, rgba(94, 210, 235, 0) 66%),
      radial-gradient(55% 140% at 90% 125%, rgba(52, 88, 180, 0.74) 0%, rgba(52, 88, 180, 0) 60%),
      linear-gradient(to top, var(--p2-bg) 0%, rgba(251, 251, 250, 0) 68%);
    -webkit-mask-image: linear-gradient(to bottom, transparent 0%, #000 32%, #000 100%);
    mask-image: linear-gradient(to bottom, transparent 0%, #000 32%, #000 100%);
  }
  :global(html[data-theme='dark']) .p2__seam {
    background:
      radial-gradient(55% 160% at 6% 130%,  rgba(96, 214, 148, 0.48) 0%, rgba(96, 214, 148, 0) 60%),
      radial-gradient(65% 170% at 42% 145%, rgba(28, 168, 152, 0.52) 0%, rgba(28, 168, 152, 0) 64%),
      radial-gradient(70% 170% at 92% 125%, rgba(35, 66, 150, 0.68) 0%, rgba(35, 66, 150, 0) 65%),
      linear-gradient(to top, var(--p2-bg) 0%, rgba(10, 10, 10, 0) 68%);
    -webkit-mask-image: linear-gradient(to bottom, transparent 0%, #000 28%, #000 100%);
    mask-image: linear-gradient(to bottom, transparent 0%, #000 28%, #000 100%);
  }

  /* ================= dark mode: bright, softly glowing headings (ref 4) ================= */
  :global(html[data-theme='dark']) .p2 h2 {
    color: #ffffff;
    text-shadow:
      0 0 14px rgba(255, 255, 255, 0.35),
      0 0 40px rgba(147, 197, 253, 0.22);
  }
  :global(html[data-theme='dark']) .p2 .bio {
    color: rgba(236, 242, 252, 0.72);
    text-shadow: 0 0 24px rgba(147, 197, 253, 0.1);
  }

  /* ---- ambient hover glow (positioned behind content) ---- */
  .p2__glow {
    position: absolute;
    top: 0; left: 0; right: 0; bottom: 0;
    pointer-events: none;
    z-index: 0;
    opacity: 0;
    transition: opacity 0.8s ease, transform 0.8s ease;
    transform: scale(0.95);
  }
  
  .p2__glow::before {
    content: "";
    position: absolute;
    left: 0%; right: 0%; top: -10%; height: 80%;
    /* Day mode: soft warm glow */
    background: radial-gradient(ellipse at center, rgba(251, 146, 60, 0.15), rgba(254, 215, 170, 0.05), transparent 60%);
    filter: blur(80px);
    animation: p2aurora 8s ease-in-out infinite alternate;
  }
  
  /* dark mode: teal -> blue ambient glow exactly like Image 3 */
  :global(html[data-theme='dark']) .p2__glow::before {
    background: radial-gradient(ellipse at center, rgba(6, 182, 212, 0.15), rgba(59, 130, 246, 0.2), transparent 70%);
    filter: blur(100px);
  }
  
  @keyframes p2aurora { 
    0% { transform: scale(1) translate(0, 0); }
    100% { transform: scale(1.05) translate(2%, 2%); } 
  }

  /* Trigger the beautiful background glow when hovering over the section */
  .p2:hover .p2__glow {
    opacity: 1;
    transform: scale(1);
  }

  .p2__inner {
    position: relative;
    z-index: 1;
    width: 100%;
    height: 100%;
  }

  /* ---- horizontal track: two full-width panels side by side.
     Desktop+motion: GSAP pins the section and slides the track -50%.
     Otherwise the panels simply stack vertically. ---- */
  .p2-track {
    display: flex;
    flex-direction: column;
    width: 100%;
    height: 100%;
    will-change: transform;
  }
  .p2-panel {
    flex-shrink: 0;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 7vh 5vw 8vh;
  }
  .p2-panel > .mac-window { max-width: 1500px; }
  @media (min-width: 1024px) and (prefers-reduced-motion: no-preference) {
    /* two panels side by side; the track slides left (parallax scale in JS) */
    .p2-track { flex-direction: row; width: 200%; }
    .p2-panel { width: 50%; }
  }

  /* deck window header title — bold + clearly visible, both themes */
  .deck-title {
    font-family: "Space Grotesk", "JetBrains Mono", monospace;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--text);
  }
  :global(html[data-theme='dark']) .deck-title {
    text-shadow: 0 0 12px rgba(147, 197, 253, 0.28);
  }

  /* ---- "Tech Stack ›››" link (racing chevrons, like the footer HERE) ---- */
  .deck-link {
    align-self: flex-start;
    margin-top: 14px;
    display: inline-flex;
    align-items: center;
    text-decoration: none;
    font-family: "Space Grotesk", sans-serif;
    font-weight: 700;
    font-size: 15px;
    letter-spacing: 0.03em;
    color: var(--accent);
    transition: text-shadow 0.3s ease, transform 0.3s ease;
  }
  .deck-link:hover {
    text-shadow: 0 0 16px var(--accent-glow);
    transform: translateX(3px);
  }
  .dl-chev { display: inline-block; margin-left: 0.05em; animation: dl-race 1.5s ease-in-out infinite; }
  .dl-c2 { animation-delay: 0.12s; }
  .dl-c3 { animation-delay: 0.24s; }
  @keyframes dl-race {
    0%, 55%, 100% { transform: translateX(0); opacity: 1; }
    25% { transform: translateX(0.3em); opacity: 0.35; }
    40% { transform: translateX(0.12em); opacity: 1; }
  }

  .mac-window {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    background: var(--mac-bg);
    border: 1px solid var(--mac-border);
    border-radius: 12px;
    box-shadow: var(--mac-shadow);
    overflow: hidden;
    transition: all 0.4s ease;
  }

  /* subtle inner-glow when hovering the mac window itself */
  .mac-window:hover {
    border-color: var(--accent-glow);
    box-shadow: var(--mac-shadow), inset 0 0 100px var(--accent-soft);
  }

  .mac-header {
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 16px;
    background: var(--mac-header-bg);
    border-bottom: 1px solid var(--mac-border);
    flex-shrink: 0;
  }

  .mac-buttons { display: flex; gap: 8px; }
  .mac-btn { width: 12px; height: 12px; border-radius: 50%; }
  .mac-btn.close { background: #ff5f56; }
  .mac-btn.minimize { background: #ffbd2e; }
  .mac-btn.maximize { background: #27c93f; }

  .mac-action { color: var(--mac-icon); display: flex; align-items: center; justify-content: center; }
  .mac-body { flex: 1; padding: 24px; overflow: hidden; display: flex; }

  .bento {
    flex: 1;
    display: grid;
    grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
    grid-template-rows: minmax(0, 1fr) minmax(0, 1fr);
    gap: 24px;
    align-items: stretch;
    width: 100%;
    height: 100%;
  }
  
  .about { grid-column: 1; grid-row: 1; min-width: 0; }
  .tech  { grid-column: 1; grid-row: 2; min-width: 0; }
  .play  { grid-column: 2; grid-row: 1 / span 2; min-width: 0; }

  .card {
    background: var(--card-bg);
    border: 1px solid var(--glass-border);
    border-radius: 16px;
    padding: 28px 32px;
    transition: transform .4s cubic-bezier(0.175, 0.885, 0.32, 1.275), box-shadow .4s ease, border-color .4s ease;
    display: flex;
    flex-direction: column;
    overflow: hidden;
  }
  
  /* the hover edge-glow for inner boxes exactly like Image 1 (yellow glow on 'About') */
  .card:hover {
    transform: translateY(-6px);
    border-color: var(--accent);
    box-shadow: 0 0 0 1px var(--accent), 0 15px 40px var(--accent-glow);
  }

  .label {
    display: flex; align-items: center; gap: 8px;
    font-family: "JetBrains Mono", monospace; font-size: 12px;
    letter-spacing: 0.18em; color: var(--text-muted); margin-bottom: 24px;
    flex-shrink: 0;
  }
  
  .dot { width: 8px; height: 8px; border-radius: 50%; background: var(--accent); }
  .dot--blue { background: #3b82f6; }

  h2 { font-family: "Clash Display","Space Grotesk",sans-serif; font-weight: 700;
       font-size: clamp(1.4rem, 2.2vw, 2.1rem); margin: 0; letter-spacing: -0.01em; color: var(--text); flex-shrink: 0; }

  .about__head { display: flex; align-items: center; gap: 16px; margin-bottom: 20px; flex-shrink: 0; }
  .avatar { width: 48px; height: 48px; border-radius: 50%; flex: none;
    background: linear-gradient(135deg, var(--ball-b), var(--ball-a)); color: #fff;
    display: grid; place-items: center; font-weight: 700; font-family: "Space Grotesk",sans-serif; font-size: 1.1rem; }

  .bio { color: var(--text-muted); line-height: 1.5; font-size: 15px; margin: 0 0 24px; max-width: 48ch; flex-shrink: 0; }

  .facts { margin: auto 0 0; display: grid; gap: 0; flex: 1; align-content: end; }
  .facts > div { display: grid; grid-template-columns: 120px 1fr; gap: 16px;
    padding: 12px 0; border-top: 1px solid var(--grid-line); align-items: baseline; }
  .facts dt { font-family: "JetBrains Mono", monospace; font-size: 11px;
    letter-spacing: 0.15em; text-transform: uppercase; color: var(--text-faint); margin: 0; }
  .facts dd { margin: 0; font-size: 13.5px; color: var(--text); }

  .marquee { margin-top: auto; overflow: hidden; flex-shrink: 0; -webkit-mask-image:
    linear-gradient(to right, transparent, #000 10%, #000 90%, transparent); }
  .marquee__track { display: flex; gap: 12px; width: max-content; animation: p2scroll 34s linear infinite; }
  .marquee:hover .marquee__track { animation-play-state: paused; }
  @keyframes p2scroll { to { transform: translateX(-50%); } }
  
  .chip { display: inline-flex; align-items: center; gap: 8px; white-space: nowrap;
    padding: 8px 14px; border: 1px solid var(--glass-border); border-radius: 999px; background: var(--mac-bg);
    font-family: "JetBrains Mono", monospace; font-size: 13px; color: var(--text); }
  .chip img { display: block; }

  .play { display: flex; flex-direction: column; }
  .play__panel { flex: 1; width: 100%; border-radius: 12px; overflow: hidden;
    background: radial-gradient(120% 120% at 50% 0%, #101a2e 0%, #0a0f1c 60%, #070a12 100%);
    display: grid; place-items: center; position: relative; }

  /* below the horizontal breakpoint (or with reduced motion) the two
     windows stack vertically and the section grows to fit */
  @media (max-width: 1023px), (prefers-reduced-motion: reduce) {
    .p2 { height: auto; min-height: 100dvh; }
    .p2__inner, .p2-track, .p2-panel { height: auto; }
    .p2-panel { min-height: 100dvh; }
  }
  @media (max-width: 900px) {
    .p2 { height: auto; min-height: 100dvh; display: block; }
    .p2__inner { height: auto; display: block; }
    .p2-panel { padding: 12vh 4vw 6vh; display: block; }
    .mac-window { height: auto; max-height: none; }
    .mac-body { padding: 16px; }
    .bento { grid-template-columns: 1fr; grid-template-rows: auto; }
    .about, .tech, .play { grid-column: 1; grid-row: auto; }
    .play { min-height: 400px; }
    .facts > div { grid-template-columns: 100px 1fr; }
  }
  @media (prefers-reduced-motion: reduce) { .marquee__track { animation: none; } }
</style>

<script>
  /* Camera handoff, page1 → page2 (scrubbed by scroll):
     - page 1 content blurs out and scales UP slightly (camera pulling past)
     - the mac window resolves from blur + 0.94 scale to crisp 1.0 (settling)
     Desktop + motion-allowed only. */
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  const mm = gsap.matchMedia();
  mm.add('(min-width: 1024px) and (prefers-reduced-motion: no-preference)', () => {
    const anims: gsap.core.Tween[] = [];

    // Blur .hero-content only — navbar/profile badge are position:fixed
    // inside the hero; filtering their ancestor breaks fixed positioning.
    const heroContent = document.querySelector('.hero .hero-content');
    if (heroContent) {
      anims.push(
        gsap.fromTo(
          heroContent,
          { filter: 'blur(0px)', opacity: 1, scale: 1 },
          {
            filter: 'blur(3px)',   // light haze, not frosted glass
            opacity: 0.8,
            scale: 1.02,           // barely-there push past page 1
            ease: 'power1.in',     // effect builds late, feels natural
            scrollTrigger: { trigger: '.p2', start: 'top bottom', end: 'top 35%', scrub: 0.6 },
          }
        )
      );
    }

    const win = document.querySelector('.p2 .mac-window');
    if (win) {
      anims.push(
        gsap.fromTo(
          win,
          // like content naturally arriving in front of you: a soft rise
          // from slightly small + hazy, settling early with an ease-out
          { filter: 'blur(4px)', opacity: 0.72, scale: 0.97, y: 28 },
          {
            filter: 'blur(0px)',
            opacity: 1,
            scale: 1,
            y: 0,
            ease: 'power2.out',    // settles gently instead of inflating linearly
            scrollTrigger: { trigger: '.p2', start: 'top 95%', end: 'top 40%', scrub: 0.6 },
          }
        )
      );
    }

    /* ---- DEPTH transition to the TECH DECK ----
       The section pins; slide 1 (bento) scales down & fades away while
       slide 2 (deck) rises OVER it, from smaller to full size — then a
       dwell so you rest on the deck before page 3. */
    const track = document.querySelector<HTMLElement>('[data-p2-track]');
    const panels = document.querySelectorAll<HTMLElement>('.p2 .p2-panel');
    const bento = panels[0], deck = panels[1];
    if (track && bento && deck) {
      gsap.set(bento, { transformOrigin: '50% 50%' });
      gsap.set(deck,  { scale: 0.9, transformOrigin: '50% 50%' });

      const tl = gsap.timeline({
        scrollTrigger: {
          id: 'p2deck', trigger: '.p2', start: 'top top',
          end: '+=118%', pin: true, scrub: 0.6, anticipatePin: 1,
        },
      });
      // simple parallax slide: track glides left; slide 1 shrinks as it
      // exits, slide 2 grows from 0.9 → 1 as it enters the viewport
      tl.to(track, { xPercent: -50, ease: 'none', duration: 1 }, 0);
      tl.to(bento, { scale: 0.9, ease: 'power1.inOut', duration: 1 }, 0);
      tl.to(deck,  { scale: 1, ease: 'power1.inOut', duration: 1 }, 0);
      tl.to({}, { duration: 0.12 });  // tiny settle, then straight to page 3
      anims.push(tl as unknown as gsap.core.Tween);
    }

    return () => anims.forEach((a) => a.scrollTrigger?.kill());
  });

  /* "Tech Stack ›››" → glide to the deck (works in every mode) */
  document.querySelector('[data-to-deck]')?.addEventListener('click', (e) => {
    e.preventDefault();
    const st = ScrollTrigger.getById('p2deck');
    if (st) {
      // land right as the deck has fully arrived
      const target = st.start + (st.end - st.start) * 0.9;
      // @ts-ignore — Lenis is exposed by Base.astro when motion is allowed
      const lenis = window.lenis;
      if (lenis) lenis.scrollTo(target);
      else window.scrollTo({ top: target, behavior: 'smooth' });
    } else {
      // stacked layout (mobile / reduced motion): plain smooth scroll
      document.querySelector('#tech-deck')?.scrollIntoView({ behavior: 'smooth' });
    }
  });
</script>