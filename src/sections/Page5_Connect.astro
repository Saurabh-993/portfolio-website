---
/**
 * Page5_Connect.astro — placeholder scroll section (100dvh).
 * TODO: contact / socials / footer.
 */
---

<section class="section placeholder" id="connect">
  <p class="label serif">Connect — coming soon</p>
</section>

<style>
  .placeholder {
    display: grid;
    place-items: center;
  }
  .label {
    font-size: clamp(1.2rem, 2.5vw, 2rem);
    color: var(--text-faint);
    font-style: italic;
  }
</style>
